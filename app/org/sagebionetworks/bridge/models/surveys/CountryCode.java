package org.sagebionetworks.bridge.models.surveys;

/**
 * The country code for the format and restrictions to be applied to the collection of a 
 * partial postal code.
 */
public enum CountryCode {
    /** Canada. */
    CA,
    /** United States. */
    US
}
